# Todo

- sped up to latest Rails (8.0.2) to launch updated version of app
	- go back to when rails was 5.2.2, upgrade to each version incrementally, step by step
	and WRITE TESTS before you START UPGRADING, for backend (rails) AND UI. THEN upgrade.
	UI tests might SUCK. but AI might be able to help you write hacky tests that work for that old React code, and then upgrade as you upgrade thru React.

- change all rails endpoint errors to use HTTP status code verbal symbols, not status code numbers.
	- e.g. `:unproccessable_entity` instead of `422`

# Doing

# Done
